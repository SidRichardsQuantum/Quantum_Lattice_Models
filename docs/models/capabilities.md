# Model Capability Matrix

This generated matrix lists logical model families once. Compatibility builder
aliases such as names ending in `_sparse` remain supported but are intentionally
hidden from ordinary discovery. Every listed model has portable geometry,
local-degree, basis-mapping, and interaction records.

{{MODEL_CAPABILITIES}}

Use `quantum-lattice models --include-aliases` to inspect compatibility aliases.

[Back to the model reference](index.md)
